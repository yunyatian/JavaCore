package com;

import java.nio.charset.StandardCharsets;
import java.util.Base64;

public class Base64EncryptionToDecrypt {
    public static void main(String args[]){
        String string = "null";
//        String encode = Base64.getEncoder().encodeToString(string.getBytes(StandardCharsets.UTF_8));
//        System.out.println(encode);
//        String decode = new String(Base64.getDecoder().decode(encode.getBytes(StandardCharsets.UTF_8)));
//        System.out.println(decode);
        string.toCharArray();
        System.out.println(string.contains("nut"));
        System.out.println(Integer.MIN_VALUE);
        char f = '1';
        System.out.println(f>='0');
    }
}
